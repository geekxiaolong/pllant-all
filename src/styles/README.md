# Archived root workspace / src/styles

> **归档说明（2026-03-09）**：`src/styles/` 为拆分前根目录单体样式目录，现仅作历史归档保留。

当前有效样式入口已经迁移至三端子仓库：

- `heart-plant/src/...`
- `heart-plant-admin/src/...`
- `heart-plant-api/`（后端无前端样式入口）

本目录下的 `index.css`、`custom.css`、`fonts.css`、`tailwind.css`、`theme.css` 均不应再被当作当前构建输入。
