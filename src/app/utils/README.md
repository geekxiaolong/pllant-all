# Archived root workspace — src/app/utils

该目录保留的是拆分前单体前端工具层历史文件，现已归档，仅供迁移追溯与差异对照。

## 当前有效实现

- 用户端工具与调用封装：`../../../../heart-plant/`
- 管理后台工具与调用封装：`../../../../heart-plant-admin/`

根目录旧文件不应再作为构建、运行或联调入口。请结合根目录 `README.md` 与 `THREE-APP-DEPLOYMENT.md` 使用三端分离结构。
