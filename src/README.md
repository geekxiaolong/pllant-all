# Archived root workspace / src

> **归档说明（2026-03-09）**：`src/` 属于拆分前的 HeartPlant 根目录单体源码树，现仅作历史归档保留。

## 当前有效入口

请不要继续把根目录 `src/` 作为运行、构建或开发入口。当前应进入以下独立仓库：

- `heart-plant/`：用户前端
- `heart-plant-admin/`：管理后台
- `heart-plant-api/`：后端 API

主导航见：

- 根目录 `README.md`
- 根目录 `START_HERE.md`
- 根目录 `THREE-APP-DEPLOYMENT.md`

## 本目录内容的定位

- `app/`：旧单体 React 页面/组件的归档残留
- `styles/`：旧单体样式入口归档
- `imports/`：历史 PRD / 导入文档归档
- `main.tsx`：已改为根工作区归档提示入口

如果需要继续开发或验证，请切换到对应子仓库，而不是恢复根目录旧单体流程。
