/**
 * Archived root workspace.
 *
 * 历史 RTSP -> WebSocket 原型已停用，根工作区不再承载当前流媒体服务。
 * 请改到三端分离后的独立仓库维护，并参考 README.md / START_HERE.md / THREE-APP-DEPLOYMENT.md。
 */

throw new Error(
  'Archived root workspace: stream-server/ffmpeg-websocket-server.ts is kept for history only. Use the split repositories instead.',
);
