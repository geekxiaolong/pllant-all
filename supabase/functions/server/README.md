# Archived root workspace — supabase/functions/server

该目录仅保留拆分前根工作区中的历史 Supabase server 入口归档，现已不再作为当前 API 实现或部署入口。

## 当前有效后端仓库

- `heart-plant-api/`

## 当前状态

- 本目录中的 `index.tsx`、`kv_store.tsx` 已改为归档守卫，仅用于阻止误运行旧根目录函数
- 若需继续 API 开发、联调、部署，请进入 `heart-plant-api/`

更多导航请见根目录：`README.md`、`START_HERE.md`、`THREE-APP-DEPLOYMENT.md`。
