# Archived root workspace — supabase/functions

该目录仅保留拆分前根工作区中的历史 Supabase Functions 归档，不再作为当前后端开发入口。

当前后端 API 的有效实现位于：

- `heart-plant-api/`

根目录中的旧函数文件仅用于历史追溯与迁移参考，已不应直接运行或部署。

更多导航请见：

- `README.md`
- `START_HERE.md`
- `THREE-APP-DEPLOYMENT.md`
