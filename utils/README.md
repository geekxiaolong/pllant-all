# Archived root workspace — utils

该目录仅保留拆分前根工作区的历史工具/配置残留，不再作为当前运行时代码入口。

当前有效代码仓库：

- `heart-plant/`：用户端源码
- `heart-plant-admin/`：管理后台源码
- `heart-plant-api/`：后端 API 源码

如需确认当前架构与入口，请查看根目录 `README.md` 与 `THREE-APP-DEPLOYMENT.md`。
