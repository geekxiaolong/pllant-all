# Archived root workspace — utils/supabase

该目录仅保留历史根工作区中的 Supabase 相关辅助文件归档，不再作为当前三端分离项目的配置真源。

当前 Supabase 配置与运行入口应以对应子仓库为准：

- `heart-plant/`
- `heart-plant-admin/`
- `heart-plant-api/`

请勿继续将根目录 `utils/supabase/` 视为当前开发入口。
